/**
 * Compatibility shim.
 *
 * Legacy OpenClaw runtime wrapper moved to `src/compat/openclaw/runtime/server.ts`.
 * Canonical Mirror service lives in `src/mirror-service/`.
 */

export { startRuntimeServer } from "../compat/openclaw/runtime/server.js";
